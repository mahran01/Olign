import React from 'react';
import { YStack } from 'tamagui';

interface ICreateChannelProps {
}

const CreateChannel: React.FC<ICreateChannelProps> = (props) => {
    return (
        <YStack f={1} w='100%' h={100} bg={'blue'}>

        </YStack>
    );
};

export default CreateChannel;
