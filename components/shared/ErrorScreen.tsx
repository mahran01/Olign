import React from 'react';
import { SafeAreaView } from 'react-native';
import { Text } from 'tamagui';

const ErrorScreen: React.FC = () => (
    <SafeAreaView>
        <Text>Error Loading Chat</Text>
    </SafeAreaView>
);

export default ErrorScreen;
